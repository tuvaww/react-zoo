import { DisplayAnimals } from "../DisplayAnimals";
import { Nav } from "../header/Nav";

export const Animals = () => {
  return (
    <section className="mainAnimalContainer">
      <DisplayAnimals />
    </section>
  );
};
